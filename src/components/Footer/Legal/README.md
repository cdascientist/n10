# FooterLegal

The legal row (`.legal`) — the four placeholder policy links plus the
© 2026 note.

**Used by:** Footer (src/components/Footer/index.jsx) — fourth child of
`.wrap`, after the hours row.

**Contains:** `div.legal` > 4 × `a` + `span.note`.

## Modify
- Change the policy links or copyright note → `index.jsx`.
