# Headline

The page-2 headline — "Tension in. / Tension out." as two masked `.ln`
lines (the `<em>` carries the italic accent). The line reveal is a
Mechanic-3 style scrub owned by effects.js.

**Used by:** IntroScene (src/components/scenes/IntroScene/index.jsx) —
first child of `.wrap.scene-in`.

**Contains:** `h1` > 2 × `span.ln` (`i` / `i>em`).

## Modify
- Change the headline copy / line structure → `index.jsx`.
- The masked line reveal is a `.ln` scrub in effects.js.
