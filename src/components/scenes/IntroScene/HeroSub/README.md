# HeroSub

The page-2 hero sub-copy paragraph (`p.hero-sub`) — the one-line pitch
for what the building holds.

**Used by:** IntroScene (src/components/scenes/IntroScene/index.jsx) —
third child of `.wrap.scene-in`, after the CTA row.

**Contains:** `p.hero-sub` — the intro copy text.

## Modify
- Rewrite the pitch → edit the text in `index.jsx`.
